# 150 Evaluate reverse Polish Notation
# use stack
# careful with - and /
# return int(num2 / num1) if num2 * num1 > 0 else -(abs(num2) // abs(num1))

# 239 Sliding Window Maximum
# append queue and clear element smaller than nums[i]
# remove queue[0] if left == queue[0]
# append queue[0] to res

# 347 Top K frequent elements
# get freq
# use size k min heap to sort
# heapq return the smallest item first


        
        